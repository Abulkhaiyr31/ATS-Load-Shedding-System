# ATS-Load-Shedding-System
ESP32 based Automatic Transfer Switch  with priority-based load shedding. Monitors dual 12V sources (grid + battery), switches automatically on voltage drop (&lt;500ms), and sheds non critical loads when battery depletes. Includes real time Python dashboard with SQLite logging.
